services:
  zapinho-api:
    build: .
    container_name: zapinho-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-9467}:${PORT:-9467}"
    volumes:
      - ./data:/app/data
