services:
  zapforge-api:
    build: .
    container_name: zapforge-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-9467}:${PORT:-9467}"
    volumes:
      - ./data:/app/data
